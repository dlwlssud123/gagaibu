# 전체 시스템 구조
- 플러터 앱은 화면과 상태 관리를 담당, 실제 데이터 처리와 AI 연동 로직은 스프링 백엔드가 담당

## 1. Presentation Layer
- 역할: 플러터 앱에서 보내는 HTTP 요청을 받고, 처리 결과를 JSON 형태로 응답
- 핵심 컴포넌트: @RestController
- 예시: 플러터에서 POST /api/v1/account-book 으로 소비내역을 보내면, 컨트롤러가 이를 받아서 서비스로 넘겨줌

## 2. Business Layer
- 역할: 가계부의 핵심 로직이 들어가는 곳. 데이터를 가공하고, AI 프롬프트를 생성하고, 외부 API를 호출하는 등의 '비즈니스'를 처리
- 핵심 컴포넌트: @Service
- 예시: "이번 달 지출 내역을 전부 모아서(DB 활용) NVIDIA NIM API에 보낼 프롬프트를 조립한 뒤, AI 리포트를 생성한다"는 로직이 위치

## 3. Data Access Layer
- 역할: DB에 직접 접근해서 데이터를 넣고 빼는 역할
- 핵심 컴포넌트: @Repository (Spring Data JPA Interface)
- 예시: findByUserIdAndDateBetween 같은 메서드로 특정 유저의 한 달치 소비 내역을 DB에서 긁어옴
## 4. Domain/Entity Layer
- 역할: DB 테이블과 1:1로 매핑되는 자바 객체들
- 예시: User, AccountBook, AiReport 등

# AI 가계부 핵심 Entity 관계 설계
초기에는 4개 엔티티
- User: 한 명의 유저는 여러 개의 지출 내역과 AI 리포트
- AccountBook
    - 금액, 카테고리, 내용, 날짜, 유형 등 저장
    - 어떤 유저의 내역인지 알아야 하므로 User와 다대일 관계
- Budget
    - 유저가 이번 달에 사용할 거라고 설정하는 목표치, AI가 예산 초과 여부를 감시할 때 기준 데이터
- AiReport
    - 생성된 리포트 내용과 생성 날짜를 저장, 매번 AI API를 호출하면 돈과 시간이 드니까, 한 번 생성된 리포트는 DB에 저장해두고 Flutter에서 조회하게 만듦
# 📂 AI 가계부 백엔드 패키지 구조 (Spring Boot)

```text
src/main/java/com/example/budget
│
├── domain
│   ├── user                          # 1. 회원 관련 도메인
│   │   ├── controller
│   │   │   └── UserController.java   # 회원가입, 로그인, 페르소나 설정 API
│   │   ├── dto
│   │   │   ├── UserJoinRequest.java
│   │   │   └── UserResponse.java
│   │   ├── entity
│   │   │   ├── User.java             # User 엔티티 (페르소나 타입 필드 포함)
│   │   │   └── PersonaType.java      # 페르소나 종류 (엄마, 츤데레, 코치 등) Enum
│   │   ├── repository
│   │   │   └── UserRepository.java
│   │   └── service
│   │       └── UserService.java
│   │
│   ├── accountbook                   # 2. 가계부 내역 관련 도메인
│   │   ├── controller
│   │   │   └── AccountBookController.java # 소비/수입 내역 CRUD API
│   │   ├── dto
│   │   │   ├── AccountBookCreateRequest.java
│   │   │   └── AccountBookResponse.java
│   │   ├── entity
│   │   │   ├── AccountBook.java      # 지출/수입 내역 엔티티 (User와 @ManyToOne)
│   │   │   └── TransactionType.java  # INCOME, EXPENDITURE 구분 Enum
│   │   ├── repository
│   │   │   └── AccountBookRepository.java # 기간별 조회, 카테고리별 합계 쿼리 포함
│   │   └── service
│   │       └── AccountBookService.java
│   │
│   ├── budget                        # 3. 월별 예산 관련 도메인
│   │   ├── controller
│   │   │   └── BudgetController.java # 월별 목표 예산 설정 API
│   │   ├── entity
│   │   │   └── Budget.java           # 예산 엔티티 (User와 @ManyToOne)
│   │   ├── repository
│   │   │   └── BudgetRepository.java
│   │   └── service
│   │       └── BudgetService.java
│   │
│   └── ai                            # 4. NVIDIA NIM 및 AI 리포트 도메인
│       ├── controller
│       │   └── AiReportController.java # AI 코칭 리포트 생성 및 내역 조회 API
│       ├── dto
│       │   └── AiReportResponse.java
│       ├── entity
│       │   └── AiReport.java         # AI 리포트 저장 엔티티 (User와 @ManyToOne)
│       ├── repository
│       │   └── AiReportRepository.java
│       ├── service
│       │   ├── AiReportService.java   # 유저 데이터 취합 및 최종 리포트 로직 생성
│       │   └── NvidiaNimService.java  # 순수 NVIDIA NIM API 호출 및 프롬프트 조립
│       └── client
│           └── NvidiaNimClient.java   # Spring AI 또는 RestClient를